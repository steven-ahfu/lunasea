name: lunasea
description: Self-Hosted Controller
version: 1.3.0
publish_to: "none"
environment:
  sdk: ">=3.9.0 <4.0.0"
  flutter: ">=3.41.0"
dependencies:
  flutter:
    sdk: flutter
  app_links: ^7.0.0
  badges: ^3.2.0
  cached_network_image: ^3.4.1
  collection: ^1.18.0
  convert: ^3.1.1
  cupertino_icons: ^1.0.8
  device_preview: ^1.3.1
  dio: ^5.9.2
  easy_localization: ^3.0.8
  encrypt: ^5.0.1
  expandable: ^5.0.1
  # 12.0.0 is still in beta (latest: 12.0.0-beta.4, published 2026-05-22) but
  # it is the only release whose `win32` constraint is on the new ^6 line,
  # matching what share_plus 13 and package_info_plus 10 require. Revert this
  # to ^11.0.2 if you need to step back to win32 ^5.
  file_picker: ^12.0.0-beta.4
  fl_chart: ^1.2.0
  flash: ^3.0.5+1
  flutter_cache_manager: ^3.3.1
  flutter_spinkit: ^5.2.2
  go_router: ^17.2.3
  google_fonts: ^8.1.0
  google_nav_bar: ^5.0.6
  hive_ce: ^2.19.3
  hive_ce_flutter: ^2.3.4
  infinite_scroll_pagination: ^5.1.1
  intl: ^0.20.2
  json_annotation: ^4.9.0
  modal_bottom_sheet: ^3.0.0-pre
  package_info_plus: ^10.1.0
  path_provider: ^2.1.0
  percent_indicator: ^4.2.5
  quick_actions: ^1.0.5
  retrofit: ^4.9.2
  share_plus: ^13.1.0
  shimmer: ^3.0.0
  simple_icons: ^14.6.1
  stack_trace: ^1.11.0
  stash_memory: ^5.2.0
  supercharged: ^2.1.1
  table_calendar: ^3.0.9
  transparent_image: ^2.0.1
  tuple: ^2.0.2
  url_launcher: ^6.3.2
  uuid: ^4.5.3
  wake_on_lan: ^4.1.0
  window_manager: ^0.5.1
  # 7.x is blocked transitively: flutter_launcher_icons -> image (<4.9.0) -> xml ^6.
  # Cannot move forward without bumping Flutter to a version that ships meta ^1.18.
  xml: ^6.6.1
dev_dependencies:
  # Generators
  build_runner: ^2.4.6
  environment_config: ^4.0.0+1
  json_serializable: ^6.9.0
  # 1.11.2 transitively requires meta ^1.18, but Flutter 3.41 pins meta 1.17.
  # Re-bump alongside a Flutter SDK upgrade.
  hive_ce_generator: ^1.11.1
  retrofit_generator: ^10.2.6
  # Tooling
  flutter_launcher_icons: ^0.14.3
  flutter_lints: ^6.0.0
  msix: ^3.16.1
dependency_overrides:
  http: ^1.6.0
flutter:
  uses-material-design: true
  assets:
    # System Assets
    - assets/images/
    - assets/localization/
  fonts:
    - family: LunaBrandIcons
      fonts:
        - asset: assets/LunaBrandIcons.ttf
flutter_icons:
  android: true
  ios: true
  image_path: assets/icon/icon.png
  adaptive_icon_background: "#32323E"
  adaptive_icon_foreground: assets/icon/icon_adaptive.png
# Splash config kept for regeneration only. flutter_native_splash is NOT a
# dependency: as a dev_dependency its Android plugin was stripped from release
# builds while GeneratedPluginRegistrant.java still referenced it, breaking any
# release build run through Gradle/Android Studio instead of the Flutter tool.
# Its output (android res launch_background.xml + styles.xml, and the iOS/web
# equivalents) is committed. To regenerate:
#   flutter pub add --dev flutter_native_splash
#   dart run flutter_native_splash:create
#   flutter pub remove flutter_native_splash
flutter_native_splash:
  image: assets/images/branding_full.png
  color: "#32323E"
msix_config:
  architecture: x64
  capabilities: internetClientServer,privateNetworkClientServer
  display_name: LunaSea
  enable_at_startup: false
  execution_alias: lunasea
  identity_name: app.dim145.lunasea
  install_certificate: false
  logo_path: assets\icon\icon_windows.png
  output_name: lunasea-windows-amd64
  store: false
